// export default function MessageBubble({ message, isMine }) {
//   return (
//     <div className={`msg ${isMine ? 'mine' : 'other'}`}>
//       {message.sender_name && (
//         <div className="msg-header">{message.sender_name}</div>
//       )}
//       <div className="msg-body">{message.content}</div>
//     </div>
//   );
// }